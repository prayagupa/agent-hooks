name: schema-drift

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "spec/schema/**"
      - "scripts/**"
      - "sdk/python/src/agent_hooks/schema/**"

jobs:
  drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: Regenerate per-point schemas and vendored copies
        run: |
          python3 scripts/gen-per-point-schemas.py
          rm -rf sdk/python/src/agent_hooks/schema
          mkdir -p sdk/python/src/agent_hooks/schema/agent-context
          cp spec/schema/*.json sdk/python/src/agent_hooks/schema/
          cp spec/schema/agent-context/*.json sdk/python/src/agent_hooks/schema/agent-context/
      - name: Fail if regeneration produced a diff
        run: git diff --exit-code
